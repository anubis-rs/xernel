pub struct Vfs {
    // mount_point_list
    // drivers (all registerd file_systems)
    // free list of vnodes
}

impl Vfs {
    // get virtual node by asking the file system driver (use mount point list to see which driver to ask)
    // wrapper functions for file syscalls thingies
}
